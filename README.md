# AI Canvas 体験用デモ環境（Vercel + Supabase）

確定 UI（[`../index.html`](../index.html)）を、そのまま実際に動く形にしたもの。
他の人が任意の写真で試せるようにするための共有環境。

ローカルの PoC 測定システム（[`../poc`](../poc)）とは**別物**で、記録も API キーも分離してある。

---

## ⚠️ 最初に読む：なぜ PoC と分けたのか

`../poc` の `npm run studio` は、**任意画像のアップロードを意図的に受け付けない**。
[`poc/runner/studio.ts`](../poc/runner/studio.ts) の冒頭にそう書いてある（関所①隔離／PoC 実装指示書 3.2）:

> 素材はマニフェストに申告済みのものだけを扱う。ブラウザからの任意ファイルのアップロードは受け付けない。
> 受け付けると、権利区分が申告されていない画像が API へ渡る経路になる。

「他の人が任意の画像で試せる」ようにするとは、この前提を外すということである。
そこで **PoC の設計を緩めるのではなく、別環境を立てた。**

| PoC が持っていた担保 | デモ環境での代替 |
| --- | --- |
| マニフェストの `rights` 区分 | アップロード者の同意チェック（`jobs.rights_confirmed` に記録） |
| `127.0.0.1` のみ待ち受け | Supabase Auth の招待制（招待外は DB のトリガが拒否） |
| 起動時に人が `--max-images` を宣言 | `demo_limits` テーブル（設定モーダルから変更可） |
| プロセス内のカウンタ | `reserve_generation()` の行ロック（サーバーレスでも守れる） |
| `output/*/results.jsonl` | `jobs` / `job_images` テーブル（**PoC の記録には混ぜない**） |

**記録を混ぜていないことが重要である。** 混ぜると、判定基準の封印済みの母数（T-01 の 10 名）に
任意画像の試行が入り込み、`config/criteria.json` の判定が意味を失う。

---

## 🔻 プロバイダ構成（2026-08-16 の実測を受けて変更）

PoC の実測（[`../poc/docs/日報_2026-08-16.md`](../poc/docs/日報_2026-08-16.md)）で次が判明した：

| | OpenAI | Google |
| --- | --- | --- |
| 成功 | **0 件 / 30 件** | 35 件 |
| 拒否のされ方 | **全被写体・全強度**（`safety_violations=[sexual]`） | 写真ごと（10 名中 8 名は生成可） |

日報の見出しはそのまま「**❌ OpenAI は使えない**」「**✅ Google は仕様どおり動く**」である。
OpenAI 専用のままでは、体験環境を配っても拒否しか表示されない。

そこで **「まず OpenAI、拒否されたら Google」** の順で試す（委託者指示）。

### 確定 UI の記述との差分（要合意）

確定 UI の設定モーダルには
「障害系エラー（5xx/タイムアウト/レート制限）のみフォールバック対象で、ポリシー起因の拒否は再投入しません」
と書いてある。しかし実測では OpenAI の失敗は**全件ポリシー拒否**であり、
この規則のままだとフォールバックが一度も発動しない。

委託者判断で**ポリシー拒否もフォールバック対象**とした。根拠：

1. 禁止事項③が禁じるのは「拒否された内容を**文言を変えて**再投入すること」であり、
   同一プロンプトを別ベンダーへ送ることはこれに当たらない
2. **拒否は課金されない**（日報：拒否・非生成の 78 件は 1 円も課金されていない）ため、
   フォールバックによる追加費用が発生しない
3. OpenAI の拒否と Google の成功が**両方**記録に残るため、データはむしろ増える
   （`job_images.attempted_provider` / `attempted_error_kind`）

管理者向けシステム設定から切り替えられる（`fallback_on_policy`）。
**同じプロバイダへの再試行は行わない**（指示書 4 章のまま）。

### 除去（マスク編集）は Gemini にも回すが、方式が違う

**OpenAI と Gemini で仕組みが別物である。** 混同すると測定を誤る。

| | OpenAI | Google（Gemini） |
| --- | --- | --- |
| 方式 | `inpaint` | `semantic_mask` |
| 範囲の伝え方 | **マスク画像そのものを API へ渡す** | 輪郭を描き込んだ目印つき画像＋位置の説明文 |
| マスク外の不変 | **仕組みで担保される** | **保証なし** |
| 出力サイズ | 指定どおり | モデルが決める（元と変わる） |

Gemini にマスク画像を渡す方法は無い。対応していた `imagen-3.0-capability-001` は
2026-06-30 に停止済みで、**2026-08-28 時点のモデル一覧にも inpaint / mask 対応は 1 つも無い**
（画像モデルはすべて `generateContent` のみ）。

そこで Gemini へは元画像と「除去範囲をマゼンタの輪郭で囲んだ画像」の 2 枚を渡し、
位置と面積を文章で添え、**目印そのものは描かないよう**指示する。

**実測（2026-08-28・合成ダミー）**：目印の写り込み 0 画素、元画像との平均画素差 3.5
（PoC の目安で再圧縮ノイズ相当）。ただし出力サイズが 768×1024 → 896×1195 に変わったため、
**「マスク外が画素単位で不変」は Gemini では原理的に成立しない。**

どちらで作られたかは `job_images.edit_method` に記録し、画面にも
「マスク指定」「範囲を説明」と表示する。
**T-02 の判定基準（マスク外に変化なしが 8 割以上）は `inpaint` にしか適用できない。**
測定目的なら管理者設定の「除去も Gemini へ回す」を切り、OpenAI 単独の結果だけを見られる。

### タトゥー除去は独立したコーナーにした

確定 UI では STEP 2 のカードの 1 つだったが、**別モードとして分離**した。

- マスク画像を入力できるのは **OpenAI だけ**で、Google へフォールバックできない
- 通常加工と同じ導線に置くと「他と同じように動くはず」という誤解を生む
- 日報は「**T-02（タトゥー除去）は測定不能が確定**。実装では解決できない」としている

コーナーには**拒否される可能性が高い旨を明示**したうえで、試せるようにしてある
（拒否は課金されず、実測データとして記録される）。
除去は 1 回 1 枚・メイクを含めない（inpaint はマスク領域の中身を指示するため、
「メイクを変えろ」を混ぜると指示が破綻する）。

---

## 🔻 確定 UI に合わせるために PoC から意図的に変えた点（3 件）

**いずれも上長・仕様書担当と共有すべき差分である。**

### ① 自由テキスト指示欄を受け付けている

確定 UI の各カードには「自由テキスト指示」欄がある。一方 PoC 実装指示書 2 章 禁止事項②は
「安全性判定の限界を探る入力をしない」とし、PoC には自由入力の経路が実装されていない。

確定 UI が確定である以上この欄は残したが、次の歯止めを付けた：

- 200 文字で切る
- 入力全文を `jobs.free_texts` に記録し、後から監査できる
- 拒否（`policy`）された場合も記録に残り、**文言を変えた再投入は行わない**

> **判断が必要**：測定の一次データとして使うなら、自由テキストを無効化する運用も選べる。
> その場合は各カードの `free-text` 入力を `disabled` にするだけで済む。

### ② メイクにテンプレート選択を通した

PoC の `buildPrompt()` は、メイクにテンプレート ID を渡すと**例外で落ちる**設計だった
（指示内容は強度で決まるため）。確定 UI のメイクカードには
テンプレート選択（ナチュラル美肌／華やかパーティー／韓国風）と「弱・中・強 各 2 枚」が**両方**ある。

つまり確定仕様では強度とスタイルは別の軸なので、拒否をやめ、
強度の指示のあとにスタイルの指示を重ねるようにした（[`build.ts`](src/lib/vendor/prompts/build.ts) の該当箇所にコメントあり）。

### ③ テンプレートのカタログを確定 UI 側に差し替えた

PoC のカタログ（背景「スタジオ（グレー背景）」など）と確定 UI の選択肢（背景「高級ラウンジ」など）は
**別物だった**。確定 UI の選択肢に合わせて [`templates.demo.ts`](src/lib/vendor/prompts/templates.demo.ts) を新設し、
画面にはこちらだけを出している。PoC 由来の定義は [`templates.ts`](src/lib/vendor/prompts/templates.ts) にそのまま残してある。

---

## そのまま引き継いだもの

- **プロバイダ層** — 送信先ホストの allowlist を含めて PoC と同一
- **プロンプトの組み立て順** — v3（何を変えるか → それ以外は保つ → 本人性の保持）
- **エラーの 3 分類**（`policy` / `input` / `infra`）— PoC の `results.jsonl` と同じ分類
- **同じプロバイダへの再試行はしない**（指示書 4 章）— 1 回呼んで失敗したらそのまま記録する
  （別ベンダーへ回すフォールバックだけは追加した。上の「プロバイダ構成」を参照）
- **テンプレート以外の指示文は API へ届かない**（自由テキストを除く）

---

## 確定 UI のうち、まだ動かない機能

画面には出ているが、この体験環境では未実装。触ると**その旨がはっきり表示される**（黙って何もしない作りにはしていない）。

| 機能 | 状態 |
| --- | --- |
| 事前モデレーションスキャン（F-04） | 未実装。拒否はプロバイダ側の判定結果として事後に出る |

なお**タトゥー除去は「未実装」ではなく、独立したコーナーとして動く**（上の「プロバイダ構成」を参照）。
ただし PoC の実測では OpenAI がこの種の素材を全件拒否しており、成功する見込みは低い。

---

## 確定処理と Google Drive 保存（STEP 5 / F-06 / 仕様書 4.7）

仕様書 2.1 STEP 5 の 3 つを実装してある。

1. **選んだ 1 枚を高解像度（2K）で再生成**し、確定画像とする
2. 確定画像を**共有ドライブ**の「店舗名 ／ キャスト名_入店年月」フォルダへ保存する
3. 使用モデル・推定コスト・処理時間・Drive ファイル ID・閲覧 URL を記録する

**既定はオフ**（`demo_limits.drive_enabled = false`）。Drive の設定が済むまで経路ごと閉じてある。
なお Drive がオフでも確定（高解像度の再生成）は行える。その場合、確定画像は Supabase 側にだけ残る。

### 高解像度の再生成について

> ⚠️ **引き伸ばしではなく作り直しである。**
> 同じプロンプトで解像度だけ上げてもう一度生成するため（仕様書 5.4 の 2 段階生成）、
> **選んだ候補と完全に同じ絵にはならない**。画面にもそう書いてある。

- ドラフトを作ったのと**同じプロバイダ・同じ方式を最初に試す**。
  別のモデルで作ると「選んだものと違う絵」が確定画像になるため
- 条件（メイク強度・バリエーション・カテゴリ・テンプレート）はドラフトと同一。
  `jobs` に残した入力から組み直している
- **必ず `reserve_generation` を通す**。ここを迂回する経路を作ってはならない
- 1 セッションにつき確定画像は 1 枚（`final_images.job_id` に UNIQUE）

> ⚠️ **2K は 1 枚あたり約 $0.85 かかる。**
> `gpt-image-2` の quality:high は 1024×1024 で $0.211、2048×2048 はその 4 倍の画素数。
> 日次予算の既定 $10 では**確定 11 回で 1 日ぶんを使い切る**。
> 押す前に画面で金額を確認させている。設定モーダルから 1K へ落とすこともでき、
> 何で作ったかは `final_images.resolution` に必ず残る。

### 認証：秘密鍵を持たない（Workload Identity 連携）

サービスアカウントの JSON 鍵は使わない。**使えない**、が正確なところで、
この組織には `iam.disableServiceAccountKeyCreation` が効いており、鍵の作成そのものが禁止されている
（Google が新しい組織へ既定で適用するもの）。

代わりに 3 段の交換を行う。

1. Vercel が実行のたびに OIDC トークンを発行する（`VERCEL_OIDC_TOKEN`）
2. Google の STS がそれを検証し、連携用の一時トークンへ交換する
3. そのトークンでサービスアカウントになりすまし、1 時間有効のアクセストークンを受け取る

**失効しない秘密がどこにも無い。** 漏れて困る値を Vercel に置かずに済む。
Drive 側の権限の持ち方は鍵方式と変わらない（共有ドライブのメンバーに入れる）。

### 仕様書 4.7.2 の異常系

| 仕様書 | 実装 |
| --- | --- |
| ① 同時作成 | `drive_locks` のリース行で「DB 確認 → Drive 作成 → DB 保存」の区間全体を覆う。その取得判定を `pg_advisory_xact_lock` で直列化し、**ロック取得後にもう一度 DB を確認**してから作成する。Drive 側でも作成の直前・直後に名前で検索し、重複が生まれていたら作成時刻の古い方を残して後発をゴミ箱へ入れる |
| ② Drive 作成後に DB 保存が失敗 | Drive を叩く**前**に `drive_folder_attempts` へ試行を記録する。DB へ書けたら `settled=true`。残った行が孤児候補で、設定モーダルに一覧が出る |
| ③ DB に ID があるが Drive 上で削除 | アップロード時の 404 を検知してフォルダを作り直し、DB を更新し、**Slack で管理者へ通知**する（過去画像は復旧できないため記録を残す） |
| ④ Drive API の一時失敗 | `final_images.drive_status = 'failed'`（＝ Drive 未同期）として記録するだけで、**UI は止めない**。画像は Supabase Storage に残っている。1 日 1 回のバッチ（`/api/cron/drive-resync`）と、設定モーダルの「未同期を今すぐ再送する」で送り直す |

**なぜロックを「テーブルの行」として持つのか。**
`pg_advisory_xact_lock` はトランザクションの終わりで必ず解放される。
ところが排他したい区間の真ん中には Drive への HTTP 呼び出しが挟まり、
PostgREST 経由ではトランザクションを跨げない。
そこで区間全体はリース行で覆い、その**取得判定そのもの**をアドバイザリロックで直列化した。
関数が途中で落ちても期限（180 秒）で自然に解放される
（解放されないと、そのキャストは二度と保存できなくなる）。

### 送るもの・送らないもの

- Drive へ送るのは**確定画像だけ**。ドラフト 6 枚は Supabase Storage に留まる。
  両方入れると、Drive を見ただけではどれが確定画像か分からなくなる
- **Slack へは画像も Drive の URL も送らない**。送るのは事故のときの店舗名・フォルダ名まで
- 送信先ホストは `sts.googleapis.com` / `iamcredentials.googleapis.com` / `www.googleapis.com`
  の 3 つに固定（`src/lib/drive.ts`）。PoC の `vendor/providers/http.ts` には足していない

### 設定のしかた

1. Google Cloud でプロジェクトを作り、**Google Drive API を有効化**
2. サービスアカウントを作成（**鍵は作らない**。IAM ロールも不要）
3. **Workload Identity プール**を作り、OIDC プロバイダを追加する
   - 発行者 URL：`https://oidc.vercel.com/<チーム slug>`
   - 対象者：`https://vercel.com/<チーム slug>`
   - 属性マッピング：`google.subject = assertion.sub`
   - 属性条件でプロジェクトを絞る（例：`assertion.sub.startsWith("owner:<チーム>:project:ai-canvas-demo")`）
4. サービスアカウントに、そのプールの主体から
   **「Workload Identity ユーザー」（`roles/iam.workloadIdentityUser`）** を付与
5. `GOOGLE_DRIVE_WIF_AUDIENCE` / `GOOGLE_DRIVE_SERVICE_ACCOUNT` を環境変数へ
6. Google Drive で**共有ドライブ**を作り、その中に保存用フォルダを作る
7. **共有ドライブのメンバーにサービスアカウントを「コンテンツ管理者」以上で追加**
   （忘れると権限エラー。アドレスは設定モーダルに表示される）
8. 保存用フォルダの URL `.../folders/<ここ>` を `GOOGLE_DRIVE_ROOT_FOLDER_ID` へ
9. 自動再送用に `CRON_SECRET` を設定（未設定だとバッチは 503 を返して何もしない）
10. 設定モーダルで「接続を確認する」→ 成功したらスイッチをオンにする

> ⚠️ **共有ドライブは Google Workspace の機能**で、Business Standard 以上でないと作れない。
> 個人の Gmail アカウントや Business Starter では、この方式は使えない。
> サービスアカウントは自身のストレージ容量を持たないため、マイドライブへは保存できない（仕様書 4.1.2）。

> ⚠️ サービスアカウントは Workspace から見て**組織外のユーザー**（`@….iam.gserviceaccount.com`）である。
> 管理コンソールで組織外との共有を止めていると、手順 7 でメンバーに追加できない。

---

## ドラフトは 4 枚（弱・中）。画面では「中」を「強」と呼ぶ

委託者指示（2026-09-16）。生成する強度を「弱・中」の 2 段階 × 各 2 枚 ＝ 4 枚に絞った。
「強」のプロンプトは使わない。画面では「中」を「強」と表示する。

- **プロンプトは一切変えていない**（`vendor/prompts/` は不変）
- **記録も変えていない**。`job_images.makeup_strength` には実際に使った段階（`weak` / `medium`）が残る
- 呼び名の対応は画面側（`AppShell` の `strengthLabel`）にだけ置く。
  表示名で記録を上書きすると、後から PoC の測定と突き合わせられなくなる
- 「強」まで作っていた頃のセッションを履歴から開いたときは、実際の段階名（弱・中・強）で出す

---

## 個別修正（F-05 逐次編集）

チャット欄に入れた指示で、選んだ 1 枚を部分的に直す。**自由文をそのまま API へ送る**（委託者判断・2026-09-16）。

- 入力は「直前の修正結果」。原本ではない（仕様書 F-05「逐次編集」）
- **別の候補を選ぶと、その候補の原本から系統をやり直す**。「やり直す」ボタンは置かず、候補の選択で決まる。次の指示が何に適用されるかは入力欄の上に出る
- 5 回を超えると警告を返す（止めはしない）
- 修正結果ごとに「ダウンロード」と「この結果で確定」が付く。確定は**その修正の指示を、その修正の入力画像に 2K で適用し直す**（仕様書 STEP 4 → STEP 5）
- 1 セッションにつき確定は 1 枚。別の画像で確定し直すと**置き換え**になる（確認を取る。Drive の古いほうはゴミ箱へ、実費はもう 1 回）
- 確定画像そのもの（2K）もダウンロードできる（`/api/jobs/[id]/download?final=1`）
- 1 回ごとに `edit_steps` へ記録。**指示の全文を切り詰めずに残す**（PoC の禁止事項②と相容れないぶん、監査できるようにしておく）
- 生成 AI に指示文を書き換えさせない（仕様書 4.5.3）。前後に「それ以外は変えない」「本人性を保つ」の枠を付けるだけ
- 上限は `reserve_generation` をそのまま通す。1 回 1 枚ぶん

実測（2026-09-16・Google）：「背景の照明を少し落として」で平均明度 114 → 82、続けて「髪を少し明るく」で髪だけ変化。各 $0.136・約 20 秒。

呼び出し間隔は 6 枚生成とは別に持つ（`demo_limits.edit_min_interval_ms`・既定 5 秒）。
`reserve_generation(…, p_kind => 'edit')` で切り替わる。全体の間隔（`global_min_interval_ms`）は種別を問わず据え置き。

---

## 参考画像（種別 B / 仕様書 4.2.5）

背景・衣装・髪型・ポーズのカードに参考画像を置ける（各 2 枚・合計 6 枚。`REFERENCE_LIMIT` と同じ）。

- **参考画像そのものを編集 API へ送る**（reference モード）。Vision で文章化して送る方式は採らない（仕様書 4.2.5 の v2.1 変更）
- 無効カテゴリの参考画像は送らない（2.2.1）。有効なカテゴリの順で連ねる
- 1024px 以内に縮めて PNG にし、`sources/<user>/<job>/ref-<category>-<n>.png` へ保存。`jobs.reference_paths` に紐付け
- 2K の作り直しにも同じ参考画像を渡す。履歴から開くと File に戻る
- 実測（2026-09-16・Google）：「明るいカフェ」テンプレート＋暖色の壁と窓の合成画像を参考に渡し、背景に暖色と窓の光が反映。$0.138・25 秒

## 店舗・キャストの台帳（F-12 / F-14 / 4.8）

`stores` / `casts` テーブル。加工画面の候補（datalist）はここから出る。
STEP 0 の「新規キャストを登録」は台帳へ入れる（誰でも可。D-2 は未確定なので体験環境では制限しない）。
管理画面（/admin → 店舗・キャスト）で追加・編集・無効化・削除。

- 表示名は「名前_YYYYMM」（4.8）。`UNIQUE (store_id, name, joined_ym)` で同月入店の同名を弾く
- **jobs の store_name / cast_name は文字列のまま**。改名・削除しても過去の記録は当時の名前で残る
- 同意状態（4.9・D-4）は持たない。体験環境は `jobs.rights_confirmed` で担保

## 管理者向けシステム設定（/admin）とヘルプ（/help）

設定はモーダルから左メニューつきのページへ。節は「上限・ブレーキ／生成・モデル／店舗・キャスト／利用者・招待／Slack 通知／確定・Drive 保存／セキュリティ」。URL のハッシュで節を覚える。
ヘルプ（/help）はログイン済みの人向けのマニュアルで、**いま動いている機能だけ**を書いてある。管理者にはさらに「管理者の方へ」の節が出る。

## 加工セッション履歴（F-07 / F-09）

左サイドバーに直近 20 件。タップすると**そのセッションを丸ごと復元**する：

- モード（通常／除去）、店舗名・キャスト名、タイトル、カテゴリの ON/OFF・テンプレート・自由文
- 元画像（署名付き URL から取り直して File にするので、**「同じ条件でもう 1 回」が押せる**）
- 候補 6 枚・修正の履歴（チャットに再掲）・確定画像・Drive のリンク
- 一覧にはセッションの実費（候補＋修正＋確定）も出す

タイトルは中央上の欄で書き換えて Enter（またはフォーカスを外す）で保存される（`PATCH /api/jobs/[id]`）。

> ⚠️ **除去のマスクは戻さない。** マスクは筆で描いた画像で、描画ツールへ読み戻す口が無い。
> 除去のセッションを開き直して再生成するには、マスクを描き直す必要がある。結果の閲覧はできる。

---

## 上限（ブレーキ）

すべて `public.demo_limits` の 1 行で決まり、**管理者向けシステム設定から変更できる**（再デプロイ不要）。

| 項目 | 既定値 |
| --- | --- |
| 生成の受付 | 稼働中（オフで全員即停止） |
| 1 日の予算 | $12.00 |
| 1 日の枚数 | 120 枚 |
| 全体の呼び出し間隔 | 10 秒 |
| 1 人あたりの呼び出し間隔 | 60 秒 |
| 招待時の既定の枚数上限 | 24 枚（＝ 4 回ぶん） |
| 1 回の生成で作る枚数 | 6 枚 |

**1 枚あたり OpenAI 約 $0.083 / Google 約 $0.136。1 回（6 枚）で約 $0.50〜$0.82。**
予約は高いほう（Google）で押さえ、実額は生成後に差し替える。$12 でおよそ 14 回。

フォールバックしても**追加費用は発生しない**（OpenAI の拒否は課金されないため、
実費は最後に成功した 1 回ぶんだけ）。

6 枚ぶんの空きが無ければ **1 枚も作らない**。画面は弱・中・強を並べて比べるためのものなので、
「予算が尽きて 3 枚だけ出来た」状態は成果物として意味を持たない。

**失敗したときの枚数の扱い**：

| 失敗の種類 | 枚数 | コスト |
| --- | --- | --- |
| `infra`（こちら側の障害） | 返す | 返す |
| `policy`（安全性判定による拒否） | **返さない** | 返す |
| `input`（送信内容の不備） | **返さない** | 返す |

`policy` で返さないのは、禁止事項③（拒否された内容を文言を変えて再投入しない）の趣旨による。
返すと、拒否されるまで何度でも押せることになる。

**全部止めたいとき**（Supabase の SQL Editor）:

```sql
update public.demo_limits set enabled = false;
```

**検証済み。** 8 通りすべてが実際に止まることを確認してある
（全体間隔・個人間隔・個人の枚数・日次枚数・日次予算・緊急停止・招待外の拒否・未招待のサインアップ拒否）。

---

## Slack への生成ログ通知

**1 回の生成が終わるたびに、枚数・料金・成否・モデルを Slack へ送る**（確定 UI の設定モーダル F-10 を実動作にしたもの）。

### 送るもの・送らないもの

| | 内容 |
| --- | --- |
| **送る** | 利用者／店舗・キャスト／加工内容とテンプレート名／成功・拒否・不備・障害の内訳／モデル別の枚数とフォールバックの有無／実費（円換算つき）／所要時間／当日累計と予算消化率 |
| **送らない** | **生成画像・元画像・署名付き URL・プロンプト全文・API キー** |

> **画像と署名付き URL を送ってはならない。**
> 署名付き URL は 1 時間有効なので、チャンネルにいる全員が顔写真を開けてしまう。
> この環境は「招待された本人と管理者だけが見られる」ことを前提に権利の同意を取っている。
> Slack へ流すとその前提が崩れる。

送信内容に画像・URL・資格情報が混ざっていないことは機械的に検査済み。
キャスト名は人物を指すラベルになりうるため、設定モーダルの
「店舗名・キャスト名を本文に含める」で落とせる。

### 送信先の固定

`hooks.slack.com` への **https** 以外へは送らない（[`src/lib/slack.ts`](src/lib/slack.ts) の `isAllowedWebhook`）。
`providers/http.ts` の `ALLOWED_HOSTS` と同じ考え方で、送信先の取り違えを塞ぐ。
`https://hooks.slack.com.evil.example.com/` のような紛らわしい URL も弾くことを確認済み。

### 設定のしかた

1. Slack で App を作成 → **Incoming Webhooks** を有効化
2. **Add New Webhook to Workspace** → 通知先チャンネルを選ぶ
3. 出てきた `https://hooks.slack.com/services/…` を環境変数 `SLACK_WEBHOOK_URL` に設定

```bash
npx vercel env add SLACK_WEBHOOK_URL production
```

設定後、**管理者向けシステム設定 →「Slack への生成ログ通知」→「テスト送信する」** で疎通を確認できる
（生成を行わないので費用はかからない）。

### 届かなかったときに気づける

送信の成否は `slack_deliveries` に必ず残り、設定モーダルに直近 5 件が表示される。
**通知の失敗で生成が壊れることはない**（例外は握り、生成結果は保存される）。

未設定でも生成そのものは動く。その場合は設定モーダルに未設定である旨が出る。

---

## 同一条件 2 枚の作り分け（T-03 に使える）

確定 UI は候補を「弱 - パターンA / 弱 - パターンB」と見せる。この 2 枚の作り分け方は
機能仕様書 4.2.4 で**未確定**（区分 A-2）であり、PoC の T-03 で決めることになっている。

設定モーダルの「同一条件 2 枚の作り分け」で切り替えられる：

- **案 2（既定）** `micro_delta` … 表情・向き・照明に軽微な差分を付ける
- **案 1** `identical` … 同一プロンプトを 2 回実行する

seed 指定は OpenAI・Google のいずれも非対応のため、案 1 の差分はモデル固有のゆらぎのみに依存する。

---

## セットアップ

### 1. Supabase（作成済み）

- プロジェクト: **ai-canvas-demo** (`fxhwiyaqhluayrknhpvi` / ap-northeast-1)
- スキーマ・RLS・Storage バケットは適用済み（[`supabase/migrations/`](supabase/migrations)）

### 2. 環境変数

`.env.example` をコピーして `.env.local` を作り、次の 2 つを埋める。

| 変数 | 取得先 | 状態 |
| --- | --- | --- |
| `SUPABASE_SERVICE_ROLE_KEY` | Supabase → Project Settings → API Keys → `service_role` | **設定済み** |
| `OPENAI_API_KEY` | **デモ専用に新規発行したキー**（PoC 用と分ける） | ★未設定 |
| `GEMINI_API_KEY` | **デモ専用・課金を有効にしたキー** | 設定済み |
| `SLACK_WEBHOOK_URL` | Slack の Incoming Webhook（任意。未設定でも生成は動く） | ★未設定 |

> **`GEMINI_API_KEY` は課金を有効にしたキーを使うこと。**
> 無料枠のままだと `quota_not_enabled` で弾かれる（PoC で実際に観測済み）。
>
> **両方設定しないと拒否しか見えない可能性が高い。** OpenAI は実測で全件拒否されている。

> **なぜキーを分けるのか**
> 同じキーだと、デモの利用が PoC の日次予算 $11.11 を食う。
> 「PoC にいくらかかったか」は測定対象そのもの（A-4）なので、混ざると測れなくなる。

### 3. ローカルで動かす

```bash
npm install
```

```bash
npm run dev
```

### 4. Vercel へデプロイ

```bash
npx vercel login
```

```bash
cd demo && npx vercel link
```

環境変数を 7 つ登録する（値は対話で聞かれる。`SLACK_WEBHOOK_URL` は任意）:

```bash
npx vercel env add NEXT_PUBLIC_SUPABASE_URL production
```

```bash
npx vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY production
```

```bash
npx vercel env add SUPABASE_SERVICE_ROLE_KEY production
```

```bash
npx vercel env add OPENAI_API_KEY production
```

```bash
npx vercel env add GEMINI_API_KEY production
```

```bash
npx vercel env add SLACK_WEBHOOK_URL production
```

```bash
npx vercel env add ADMIN_EMAILS production
```

本番へ出す:

```bash
npx vercel --prod
```

> **Vercel のプランに注意**：6 枚の生成を 1 回の関数呼び出し（最大 300 秒）で処理する。
> Fluid compute が有効なプランが必要。無効だと 6 枚そろう前に関数が打ち切られ、
> 残りのスロットが「失敗（infra）」として記録される（枚数は返却される）。

### 5. デプロイ後に必ず行う設定

**Supabase ダッシュボード → Authentication → URL Configuration**

- **Site URL** に Vercel の本番 URL（`https://....vercel.app`）
- **Redirect URLs** に `https://....vercel.app/auth/callback`

これをしないと、メールのログインリンクが `localhost` へ飛んで動かない。

---

## 招待のしかた

`ADMIN_EMAILS` のアドレスでログインすると、左下の「⚙️ 管理者向けシステム設定」が出る。
その中の「利用者の招待」でメールアドレスと枚数上限を入れて招待する。

**「パスワードを発行する」を推奨する。** Supabase の組み込みメール送信には厳しい回数制限がある
（独自 SMTP を設定するまでは、パスワードを発行して直接伝えるほうが確実）。

招待は 2 段構えで、片方だけでは入れない：

1. `allowed_emails` へ登録（DB のトリガが、載っていないアドレスの作成を拒否する）
2. Auth のユーザーを作成（ログイン画面は `shouldCreateUser: false` で動く）

---

## 本番 URL

**https://ai-canvas-demo.vercel.app**

- Vercel プロジェクト: `novel-innovation/ai-canvas-demo`
- 環境変数 6 つ登録済み。`NEXT_PUBLIC_SUPABASE_ANON_KEY` は Vercel の警告どおり
  **公開型（config）** で登録している。Supabase の anon キーはブラウザへ渡る前提の公開鍵で、
  保護は RLS が担う（全テーブル RLS 有効／上限系テーブルはポリシーゼロ＝anon から読めない／
  Storage は非公開バケット）。

### 本番での疎通確認（2026-08-28）

| 確認項目 | 結果 |
| --- | --- |
| ログイン（パスワード） | 成功。管理者として認識される |
| 1 枚生成 | 成功 27.2 秒 / $0.0488 |
| **6 枚生成** | **6/6 成功・全体 62 秒・$0.2931** |

**6 枚で 62 秒かかり、それが完走した。** つまり関数の実行時間は 60 秒を超えて確保されており、
Fluid compute の懸念は解消している。`after()` による応答後の背景処理も本番で完走する。

---

## 疎通確認の結果（ローカル・実 API）

**すべて成功した。** 合成ダミー画像での確認。

| 確認項目 | 結果 |
| --- | --- |
| OpenAI（通常加工） | 成功 36.7 秒 / $0.0488 |
| OpenAI（除去・マスク編集） | 成功 30.6 秒 / $0.0485 |
| Google（Gemini） | 成功 18.3 秒 / $0.136（`quota_not_enabled` は出ず） |
| **フォールバック** | OpenAI を失敗させると Google が引き受け、両方が記録に残ることを確認 |
| **6 枚ジョブ** | 6/6 成功・全体 67 秒・合計 $0.294（同時 3 本 → 残り 3 本が待機） |

**注意：この確認では OpenAI に拒否されなかった。**
合成ダミーは PoC が拒否された実素材（在籍キャストの写真）とは別物であり、
実際の写真では日報どおり拒否される可能性が高い。
**フォールバックが本番で効くかどうかは、実素材で試すまで確定しない。**

実測されたコスト（表からの見積より安かった）：

| | 見積 | 実測 |
| --- | --- | --- |
| OpenAI 1 枚 | $0.083 | **$0.049** |
| Google 1 枚 | $0.136 | **$0.136** |
| 6 枚（全部 OpenAI） | $0.50 | **$0.294** |

---

## 構築の状況（2026-08-28 時点）

**環境の構築は完了している。** 以下はすべて本番で実際に動かして確認済み。

| # | 項目 | 状態 |
| --- | --- | --- |
| 1 | API キー（OpenAI / Gemini）の設定 | **完了**・疎通確認済み |
| 2 | Vercel へのデプロイ | **完了** https://ai-canvas-demo.vercel.app |
| 3 | 実 API での生成（1 枚 / 6 枚 / 除去 / フォールバック） | **完了** |
| 4 | Slack への生成ログ通知 | **完了**・テスト送信が HTTP 200 |
| 5 | メールのログインリンク（Site URL / Redirect URLs） | **完了**・実際にリンクからログインできることを確認 |

### 残っているのは運用の判断

| # | 内容 |
| --- | --- |
| A | **「意図的に変えた点」の合意**（①自由テキスト欄、②ポリシー拒否でのフォールバック）。上の該当節を参照 |
| B | 他の方の招待（管理者向けシステム設定 →「利用者の招待」） |
| C | 独自 SMTP の設定。**メールのログインリンクを常用する場合のみ**。Supabase の組み込みメールは連続送信に厳しい制限があり、複数人が同時にログインすると詰まる。パスワード発行方式なら不要 |
| D | PoC 側で `maskSemantics` が実測確定したら [`src/lib/models.ts`](src/lib/models.ts) を揃える |

### 実素材でしか確定しないこと

疎通確認はすべて**合成ダミー画像**で行っており、**OpenAI に一度も拒否されなかった**。
PoC の日報が報告する拒否（30/30・`safety_violations=[sexual]`）は実素材でのものなので、
実際の写真では拒否される可能性が高いままである。

フォールバックの配線が動くことは（OpenAI を意図的に失敗させて）確認済みだが、
**本番で実際にフォールバックが必要になるか・効くかは、実素材で試すまで確定しない。**
